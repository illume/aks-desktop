From a2d81acd06bd934b3c281ed1e2d99b514e9c1e4a Mon Sep 17 00:00:00 2001
From: "copilot-swe-agent[bot]" <198982749+Copilot@users.noreply.github.com>
Date: Fri, 31 Jul 2026 14:18:54 +0000
Subject: [PATCH] aksd: drop upstreamed map-label reflow fix

---
 .../resourceMap/nodes/KubeObjectNode.tsx      | 30 +++++++++----------
 1 file changed, 14 insertions(+), 16 deletions(-)

diff --git a/frontend/src/components/resourceMap/nodes/KubeObjectNode.tsx b/frontend/src/components/resourceMap/nodes/KubeObjectNode.tsx
index d2f4c1bb6..600ea06be 100644
--- a/frontend/src/components/resourceMap/nodes/KubeObjectNode.tsx
+++ b/frontend/src/components/resourceMap/nodes/KubeObjectNode.tsx
@@ -57,17 +57,6 @@ const Container = styled('div')<{
   ':hover': {
     borderColor: isSelected ? undefined : alpha(theme.palette.action.active, 0.2),
     boxShadow: '4px 4px 6px rgba(0,0,0,0.06)',
-    zIndex: 10,
-    '& .label-container': {
-      overflow: 'visible',
-    },
-  },
-
-  ':focus-within': {
-    zIndex: 10,
-    '& .label-container': {
-      overflow: 'visible',
-    },
   },
 
   '::before':
@@ -129,7 +118,7 @@ const TextContainer = styled('div')({
   display: 'flex',
   alignItems: 'center',
   gap: '8px',
-  minHeight: '48px',
+  height: '48px',
 });
 
 const LabelContainer = styled('div')<{ isCollapsed?: boolean }>(({ isCollapsed }) => ({
@@ -146,8 +135,9 @@ const Subtitle = styled('div')({
 });
 
 const Title = styled('div')({
-  whiteSpace: 'normal',
-  wordBreak: 'break-word',
+  textOverflow: 'ellipsis',
+  overflow: 'hidden',
+  whiteSpace: 'nowrap',
 });
 
 const EXPAND_DELAY = 450;
@@ -278,9 +268,17 @@ export const KubeObjectNodeComponent = memo(({ id }: NodeProps) => {
 
       <TextContainer>
         {icon}
-        <LabelContainer className="label-container" isCollapsed={isCollapsed}>
+        <LabelContainer isCollapsed={isCollapsed}>
           <Subtitle>{subtitle}</Subtitle>
-          <Title>{label}</Title>
+          <Title
+            sx={{
+              textOverflow: 'ellipsis',
+              overflow: 'hidden',
+              whiteSpace: 'nowrap',
+            }}
+          >
+            {label}
+          </Title>
         </LabelContainer>
       </TextContainer>
       {isExpanded && <NodeGlance node={node} />}
-- 
2.54.0

