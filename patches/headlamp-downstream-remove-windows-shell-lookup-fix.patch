From 1dfba6eb1d355b293e147fba86b14debaf503ca7 Mon Sep 17 00:00:00 2001
From: "copilot-swe-agent[bot]" <198982749+Copilot@users.noreply.github.com>
Date: Fri, 31 Jul 2026 15:16:02 +0000
Subject: [PATCH] aksd: drop upstreamed Windows shell lookup fix

---
 app/electron/main.ts | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/app/electron/main.ts b/app/electron/main.ts
index 261643b26..3c35c3f0f 100644
--- a/app/electron/main.ts
+++ b/app/electron/main.ts
@@ -748,6 +748,7 @@ async function getShellEnvWin(): Promise<NodeJS.ProcessEnv> {
 
 export async function getShellEnv(): Promise<NodeJS.ProcessEnv> {
   const execPromisify = promisify(exec);
+  const shell = await getShell();
   const isWindows = process.platform === 'win32';
 
   // For Windows, just return the current environment
@@ -755,8 +756,7 @@ export async function getShellEnv(): Promise<NodeJS.ProcessEnv> {
     return { ...process.env };
   }
 
-  // For Unix-like systems, get the shell
-  const shell = await getShell();
+  // For Unix-like systems
   const isZsh = shell.includes('zsh');
   // interactive is supported only on zsh
   const shellArgs = isZsh ? ['--login', '--interactive', '-c'] : ['--login', '-c'];
-- 
2.54.0

