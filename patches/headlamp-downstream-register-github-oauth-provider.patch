From 11d15779faff4ba726ae6297d0cc46dac483ea6c Mon Sep 17 00:00:00 2001
From: "copilot-swe-agent[bot]" <198982749+Copilot@users.noreply.github.com>
Date: Fri, 31 Jul 2026 21:52:32 +0000
Subject: [PATCH] aksd: register GitHub OAuth provider

---
 app/electron/github-oauth.test.ts | 78 ++++++++++++++++++++++++++-----
 app/electron/github-oauth.ts      | 28 +++++++++--
 app/electron/main.ts              | 52 ++++++++-------------
 3 files changed, 109 insertions(+), 49 deletions(-)

diff --git a/app/electron/github-oauth.test.ts b/app/electron/github-oauth.test.ts
index 8146467ad..d593a0b84 100644
--- a/app/electron/github-oauth.test.ts
+++ b/app/electron/github-oauth.test.ts
@@ -27,7 +27,13 @@ vi.mock('electron', () => ({
 }));
 
 vi.mock('./secure-storage', () => ({
-  handleSecureStorageSave: vi.fn(() => ({ success: true })),
+  PluginSecureStorage: vi.fn(() => ({
+    save: vi.fn(() => ({ success: true })),
+  })),
+}));
+
+vi.mock('./oauth-provider', () => ({
+  registerOAuthProvider: vi.fn(() => vi.fn()),
 }));
 
 import { ipcMain, shell } from 'electron';
@@ -40,10 +46,12 @@ import {
   handleOAuthCallback,
   setPendingState,
   setupGitHubOAuthHandlers,
-  STORAGE_KEY,
+  STORAGE_ENTRY_KEY,
+  STORAGE_NAMESPACE,
 } from './github-oauth';
 import { GITHUB_OAUTH_CALLBACK, GITHUB_OAUTH_REFRESH, GITHUB_OAUTH_START } from './ipc-channels';
-import { handleSecureStorageSave } from './secure-storage';
+import { registerOAuthProvider } from './oauth-provider';
+import { PluginSecureStorage } from './secure-storage';
 
 /** Saves original fetch so tests can restore it after spying. */
 const originalFetch = global.fetch;
@@ -68,6 +76,14 @@ function restoreFetch() {
 }
 
 const TEST_REDIRECT_URI = 'test-app://oauth/callback';
+const saveToSecureStorage = vi.fn(() => ({ success: true }));
+
+function resetSecureStorageMock() {
+  saveToSecureStorage.mockClear();
+  (PluginSecureStorage as Mock).mockImplementation(() => ({
+    save: saveToSecureStorage,
+  }));
+}
 
 describe('handleOAuthCallback', () => {
   let mockMainWindow: ReturnType<typeof createMockMainWindow>;
@@ -75,7 +91,7 @@ describe('handleOAuthCallback', () => {
   beforeEach(() => {
     mockMainWindow = createMockMainWindow();
     setPendingState(null);
-    (handleSecureStorageSave as Mock).mockClear();
+    resetSecureStorageMock();
     mockMainWindow.webContents.send.mockClear();
     mockMainWindow.focus.mockClear();
   });
@@ -159,9 +175,9 @@ describe('handleOAuthCallback', () => {
     const url = new URL('test-app://oauth/callback?code=code-456&state=valid-state');
     await handleOAuthCallback(url, mockMainWindow, TEST_REDIRECT_URI);
 
-    expect(handleSecureStorageSave).toHaveBeenCalledWith(
-      expect.stringContaining('secure-storage.json'),
-      STORAGE_KEY,
+    expect(saveToSecureStorage).toHaveBeenCalledWith(
+      STORAGE_NAMESPACE,
+      STORAGE_ENTRY_KEY,
       expect.stringContaining('"accessToken":"ghu_access"')
     );
   });
@@ -205,7 +221,7 @@ describe('github-oauth-refresh handler', () => {
     // Register handlers and capture them
     const mockIpcMain = ipcMain as any;
     mockIpcMain.handle.mockClear();
-    (handleSecureStorageSave as Mock).mockClear();
+    resetSecureStorageMock();
 
     setupGitHubOAuthHandlers();
 
@@ -245,9 +261,9 @@ describe('github-oauth-refresh handler', () => {
 
     await refreshHandler({}, { refreshToken: 'ghr_old' });
 
-    expect(handleSecureStorageSave).toHaveBeenCalledWith(
-      expect.stringContaining('secure-storage.json'),
-      STORAGE_KEY,
+    expect(saveToSecureStorage).toHaveBeenCalledWith(
+      STORAGE_NAMESPACE,
+      STORAGE_ENTRY_KEY,
       expect.stringContaining('"accessToken":"ghu_refreshed"')
     );
   });
@@ -283,6 +299,44 @@ describe('github-oauth-refresh handler', () => {
   });
 });
 
+describe('OAuth provider registration', () => {
+  it('registers the product callback with the host registry', async () => {
+    const mainWindow = createMockMainWindow();
+    setupGitHubOAuthHandlers({
+      getMainWindow: () => mainWindow,
+      protocolScheme: 'aks-desktop',
+    });
+
+    expect(registerOAuthProvider).toHaveBeenCalledWith({
+      id: 'aks-desktop.github',
+      callback: { hostname: 'oauth', pathname: '/callback' },
+      handleCallback: expect.any(Function),
+    });
+
+    const registration = (registerOAuthProvider as Mock).mock.calls.at(-1)?.[0];
+    setPendingState('registered-state');
+    mockFetchResponse({
+      access_token: 'ghu_registered',
+      refresh_token: 'ghr_registered',
+      expires_in: 3600,
+    });
+
+    await registration.handleCallback(
+      new URL('aks-desktop://oauth/callback?code=registered-code&state=registered-state')
+    );
+
+    expect(global.fetch).toHaveBeenCalledWith(
+      expect.any(String),
+      expect.objectContaining({
+        body: expect.stringContaining(
+          'redirect_uri=' + encodeURIComponent('aks-desktop://oauth/callback')
+        ),
+      })
+    );
+    restoreFetch();
+  });
+});
+
 describe('dev callback server', () => {
   let startHandler: (...args: any[]) => any;
   let mockMainWindow: ReturnType<typeof createMockMainWindow>;
@@ -291,7 +345,7 @@ describe('dev callback server', () => {
     mockMainWindow = createMockMainWindow();
     const mockIpcMain = ipcMain as any;
     mockIpcMain.handle.mockClear();
-    (handleSecureStorageSave as Mock).mockClear();
+    resetSecureStorageMock();
 
     setupGitHubOAuthHandlers({ isDev: true, getMainWindow: () => mockMainWindow });
 
diff --git a/app/electron/github-oauth.ts b/app/electron/github-oauth.ts
index 3c77eeb2c..629187e83 100644
--- a/app/electron/github-oauth.ts
+++ b/app/electron/github-oauth.ts
@@ -30,7 +30,8 @@ import { app, ipcMain, shell } from 'electron';
 import http from 'http';
 import path from 'path';
 import { GITHUB_OAUTH_CALLBACK, GITHUB_OAUTH_REFRESH, GITHUB_OAUTH_START } from './ipc-channels';
-import { handleSecureStorageSave } from './secure-storage';
+import { registerOAuthProvider } from './oauth-provider';
+import { PluginSecureStorage } from './secure-storage';
 
 export const CLIENT_ID = 'Iv23liWWbvrfIrA6WWj5';
 // The client secret is embedded intentionally. Native/desktop apps are "public clients" that
@@ -56,6 +57,8 @@ interface GitHubTokenResponse {
 
 /** Module-scoped pending CSRF state (single pending flow at a time). */
 let pendingState: string | null = null;
+export const STORAGE_NAMESPACE = STORAGE_KEY.slice(0, STORAGE_KEY.indexOf(':'));
+export const STORAGE_ENTRY_KEY = STORAGE_KEY.slice(STORAGE_NAMESPACE.length + 1);
 
 /** Visible for testing — returns the current pending state. */
 export function getPendingState(): string | null {
@@ -78,9 +81,9 @@ export function setPendingState(state: string | null): void {
  */
 function saveTokens(accessToken: string, refreshToken: string, expiresAt: string): void {
   const storagePath = path.join(app.getPath('userData'), 'secure-storage.json');
-  const result = handleSecureStorageSave(
-    storagePath,
-    STORAGE_KEY,
+  const result = new PluginSecureStorage(storagePath).save(
+    STORAGE_NAMESPACE,
+    STORAGE_ENTRY_KEY,
     JSON.stringify({ accessToken, refreshToken, expiresAt })
   );
   if (!result.success) {
@@ -90,6 +93,7 @@ function saveTokens(accessToken: string, refreshToken: string, expiresAt: string
 
 /** Active dev callback server, if any. */
 let devServer: http.Server | null = null;
+let unregisterOAuthProvider: (() => void) | null = null;
 
 /** Visible for testing — returns the active dev server. */
 export function getDevServer(): http.Server | null {
@@ -161,6 +165,22 @@ export function setupGitHubOAuthHandlers(options?: {
   const prodRedirectUri = `${options?.protocolScheme ?? 'headlamp'}://oauth/callback`;
   const redirectUri = isDev ? DEV_REDIRECT_URI : prodRedirectUri;
 
+  unregisterOAuthProvider?.();
+  unregisterOAuthProvider = null;
+  if (!isDev) {
+    unregisterOAuthProvider = registerOAuthProvider({
+      id: 'aks-desktop.github',
+      callback: { hostname: 'oauth', pathname: '/callback' },
+      async handleCallback(url) {
+        const mainWindow = getMainWindow();
+        if (!mainWindow) {
+          throw new Error('Main window not available');
+        }
+        await handleOAuthCallback(url, mainWindow, prodRedirectUri);
+      },
+    });
+  }
+
   ipcMain.handle(GITHUB_OAUTH_START, async () => {
     try {
       const state = crypto.randomUUID();
diff --git a/app/electron/main.ts b/app/electron/main.ts
index a3fbfc6d5..e1f891cf2 100644
--- a/app/electron/main.ts
+++ b/app/electron/main.ts
@@ -42,9 +42,10 @@ import path from 'path';
 import url from 'url';
 import yargs from 'yargs';
 import { hideBin } from 'yargs/helpers';
-import { handleOAuthCallback, setupGitHubOAuthHandlers } from './github-oauth';
+import { setupGitHubOAuthHandlers } from './github-oauth';
 import i18n from './i18next.config';
 import MCPClient from './mcp/MCPClient';
+import { dispatchOAuthCallback } from './oauth-provider';
 import {
   addToPath,
   ArtifactHubHeadlampPkg,
@@ -54,7 +55,7 @@ import {
   getPluginBinDirectories,
   PluginManager,
 } from './plugin-management';
-import { readProtocolScheme } from './protocol';
+import { isProtocolUrl, readProtocolScheme } from './protocol';
 import { addRunCmdConsent, removeRunCmdConsent, runScript, setupRunCmdHandlers } from './runCmd';
 import { setupSecureStorageHandlers } from './secure-storage';
 import { cleanupHeadlampTray, createHeadlampTray } from './tray';
@@ -1706,6 +1707,7 @@ async function startElectron() {
       }
     });
 
+    let initialOAuthCallbackHandled = false;
     mainWindow.webContents.on('did-finish-load', async () => {
       const startZoom = await loadZoomFactor();
       if (startZoom !== 1.0) {
@@ -1715,13 +1717,10 @@ async function startElectron() {
       // Inject the backend port into the window object
       mainWindow?.webContents.executeJavaScript(`window.headlampBackendPort = ${actualPort};`);
 
-      // aksd: On Windows/Linux cold start, deep link URLs arrive via process.argv
-      // (not via second-instance). Check initial argv after the window is ready.
-      if (process.platform !== 'darwin') {
-        const deepLink = process.argv.find(arg => arg.startsWith(`${protocolScheme}://`));
-        if (deepLink) {
-          handleDeepLink(deepLink, mainWindow);
-        }
+      if (!initialOAuthCallbackHandled && process.platform !== 'darwin') {
+        const callbackUrl = process.argv.find(value => isProtocolUrl(value, protocolScheme));
+        initialOAuthCallbackHandled =
+          callbackUrl !== undefined && dispatchOAuthCallback(new URL(callbackUrl), protocolScheme);
       }
     });
 
@@ -1763,24 +1762,6 @@ async function startElectron() {
       }
     });
 
-    /** Routes deep links matching the app's registered protocol scheme to the appropriate handler. */
-    function handleDeepLink(urlString: string, targetWindow: BrowserWindow | null): boolean {
-      if (!targetWindow) return false;
-      try {
-        const urlObj = new URL(urlString);
-        if (urlObj.protocol !== `${protocolScheme}:`) {
-          return false;
-        }
-        if (urlObj.hostname === 'oauth' && urlObj.pathname === '/callback') {
-          handleOAuthCallback(urlObj, targetWindow, `${protocolScheme}://oauth/callback`);
-          return true;
-        }
-      } catch {
-        // Ignore malformed URLs
-      }
-      return false;
-    }
-
     // Force Single Instance Application
     const gotTheLock = app.requestSingleInstanceLock();
     if (gotTheLock) {
@@ -1791,10 +1772,9 @@ async function startElectron() {
           mainWindow.focus();
         }
 
-        // aksd: On Windows/Linux, deep link URLs arrive as the last argv element
-        const deepLink = argv.find(arg => arg.startsWith(`${protocolScheme}://`));
-        if (deepLink) {
-          handleDeepLink(deepLink, mainWindow);
+        const callbackUrl = argv.find(value => isProtocolUrl(value, protocolScheme));
+        if (callbackUrl) {
+          dispatchOAuthCallback(new URL(callbackUrl), protocolScheme);
         }
       });
     } else {
@@ -1828,8 +1808,14 @@ async function startElectron() {
         return;
       }
 
-      // aksd: Intercept OAuth callback deep links
-      if (handleDeepLink(url, mainWindow)) {
+      if (!isProtocolUrl(url, protocolScheme)) {
+        dialog.showErrorBox(
+          i18n.t('Invalid URL'),
+          i18n.t('Application opened with an invalid URL: {{ url }}', { url })
+        );
+        return;
+      }
+      if (dispatchOAuthCallback(urlObj, protocolScheme)) {
         return;
       }
 
-- 
2.54.0

