diff --git a/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.test.tsx b/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.test.tsx
index d9f7179d8..4b40dcf2c 100644
--- a/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.test.tsx
+++ b/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.test.tsx
@@ -30,6 +30,10 @@ import {
 } from './useKubeObjectList';
 import * as websocket from './webSocket';
 
+const { mockGetAllowedNamespaces } = vi.hoisted(() => ({
+  mockGetAllowedNamespaces: vi.fn(() => [] as string[]),
+}));
+
 // Mock WebSocket functionality
 const mockUseWebSockets = vi.fn();
 const mockSubscribe = vi.fn().mockImplementation(() => Promise.resolve(() => {}));
@@ -50,6 +54,10 @@ vi.mock('./fetch', () => ({
   clusterFetch: vi.fn(),
 }));
 
+vi.mock('../../cluster', () => ({
+  getAllowedNamespaces: mockGetAllowedNamespaces,
+}));
+
 describe('makeListRequests', () => {
   describe('for non namespaced resource', () => {
     it('should not include namespace in requests', () => {
@@ -369,6 +377,47 @@ describe('useKubeObjectList', () => {
     vi.clearAllMocks();
   });
 
+  it('fetches allowed namespaces individually without starting a cluster-wide watch', async () => {
+    const namespaceClass = class {
+      static apiVersion = 'v1';
+      static apiName = 'namespaces';
+      static kind = 'Namespace';
+
+      constructor(public jsonData: any) {}
+    } as any;
+    mockGetAllowedNamespaces.mockReturnValue(['team-a', 'team-b']);
+    mockClusterFetch
+      .mockResolvedValueOnce({
+        json: () => Promise.resolve({ metadata: { name: 'team-a' } }),
+      } as Response)
+      .mockResolvedValueOnce({
+        json: () => Promise.resolve({ metadata: { name: 'team-b' } }),
+      } as Response);
+
+    const query = kubeObjectListQuery(
+      namespaceClass,
+      { version: 'v1', resource: 'namespaces' },
+      undefined,
+      'restricted',
+      {}
+    );
+    const response = await (query.queryFn as any)();
+    const items = response.list.items as Array<{
+      cluster: string;
+      jsonData: { metadata: { name: string } };
+    }>;
+
+    expect(mockClusterFetch).toHaveBeenNthCalledWith(1, 'api/v1/namespaces/team-a', {
+      cluster: 'restricted',
+    });
+    expect(mockClusterFetch).toHaveBeenNthCalledWith(2, 'api/v1/namespaces/team-b', {
+      cluster: 'restricted',
+    });
+    expect(items.map(item => item.jsonData.metadata.name)).toEqual(['team-a', 'team-b']);
+    expect(items.every(item => item.cluster === 'restricted')).toBe(true);
+    expect(response.skipWatch).toBe(true);
+  });
+
   it('should not add a list limit unless the caller opts in', async () => {
     mockClusterFetch.mockResolvedValueOnce({
       json: () => Promise.resolve(makeListResponse()),
diff --git a/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.ts b/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.ts
index 53e47416f..f972ddc25 100644
--- a/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.ts
+++ b/source/frontend/src/lib/k8s/api/v2/useKubeObjectList.ts
@@ -17,6 +17,7 @@
 import type { QueryObserverOptions } from '@tanstack/react-query';
 import { useQueries, useQueryClient } from '@tanstack/react-query';
 import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
+import { getAllowedNamespaces } from '../../cluster';
 import type { KubeObject, KubeObjectClass } from '../../KubeObject';
 import type { QueryParameters } from '../v1/queryParameters';
 import { ApiError } from './ApiError';
@@ -63,6 +64,54 @@ export interface ListResponse<K extends KubeObject> {
   cluster: string;
   /** If the list only has items from one namespace */
   namespace?: string;
+  /** Whether this synthesized list must not start a cluster-wide watch */
+  skipWatch?: boolean;
+}
+
+function allowedNamespaceListQuery<K extends KubeObject>(
+  kubeObjectClass: KubeObjectClass,
+  cluster: string,
+  refetchInterval?: number
+): QueryObserverOptions<ListResponse<K> | undefined | null, ApiError> {
+  const allowedNamespaces = getAllowedNamespaces(cluster);
+
+  return {
+    placeholderData: null,
+    refetchInterval,
+    retry: kubeRequestRetry,
+    queryKey: [
+      'kubeObject',
+      'list',
+      kubeObjectClass.apiVersion,
+      kubeObjectClass.apiName,
+      cluster,
+      '',
+      { allowedNamespaces },
+    ],
+    queryFn: async () => {
+      const items = await Promise.all(
+        allowedNamespaces.map(async name => {
+          const item = await clusterFetch(makeUrl(['api', 'v1', 'namespaces', name]), {
+            cluster,
+          }).then(response => response.json());
+          const kubeObject = new kubeObjectClass(item) as K;
+          kubeObject.cluster = cluster;
+          return kubeObject;
+        })
+      );
+
+      return {
+        list: {
+          items,
+          kind: 'NamespaceList',
+          apiVersion: 'v1',
+          metadata: { resourceVersion: '0' },
+        } as KubeList<K>,
+        cluster,
+        skipWatch: true,
+      };
+    },
+  };
 }
 
 /**
@@ -83,6 +132,10 @@ export function kubeObjectListQuery<K extends KubeObject>(
   queryParams: QueryParameters,
   refetchInterval?: number
 ): QueryObserverOptions<ListResponse<K> | undefined | null, ApiError> {
+  if (kubeObjectClass.kind === 'Namespace' && getAllowedNamespaces(cluster).length > 0) {
+    return allowedNamespaceListQuery<K>(kubeObjectClass, cluster, refetchInterval);
+  }
+
   return {
     placeholderData: null,
     refetchInterval,
@@ -663,11 +716,13 @@ export function useKubeObjectList<K extends KubeObject>({
           : keptListsToWatch;
       }
 
-      const nextListsToWatch = query.data.filter(Boolean).map(data => ({
-        cluster: data!.cluster,
-        namespace: data!.namespace,
-        resourceVersion: data!.list.metadata.resourceVersion,
-      }));
+      const nextListsToWatch = query.data
+        .filter(data => data && !data.skipWatch)
+        .map(data => ({
+          cluster: data!.cluster,
+          namespace: data!.namespace,
+          resourceVersion: data!.list.metadata.resourceVersion,
+        }));
 
       if (
         nextListsToWatch.length === currentListsToWatch.length &&
